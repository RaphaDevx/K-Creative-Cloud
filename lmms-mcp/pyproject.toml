[project]
name = "lmms-mcp"
version = "0.1.0"
description = "LMMS MCP — CLI rendering, project listing, sample browser"
requires-python = ">=3.10"
dependencies = ["mcp[cli]>=1.3.0", "fastmcp>=2.0.0"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
